# 朝活トラッカー｜スプレッドシート連携 セットアップ手順

この手順で、コックピットの「記録する」が**既存スプレッドシートに自動で書き込まれ**、
トラッカーの草グラフ・今日のテーマも**シートから自動取得**されるようになります。

所要時間：5〜10分（コピペとボタン操作だけ）

---

## ステップ1：Apps Script を開く

1. 対象のスプレッドシートを開く
   （https://docs.google.com/spreadsheets/d/1v9KZpqdr-OGBLCblEU3ZGbbGsm3wryFmc5rZhlhd7B4/edit）
2. 上部メニュー **拡張機能 → Apps Script** をクリック

## ステップ2：コードを貼る

1. 開いたエディタの既存コード（`function myFunction() {}` など）を**全部消す**
2. このフォルダの **`gas/Code.gs`** の中身を**全部コピーして貼り付け**
3. 💾（保存）をクリック

> 補足：シート名や列の位置は**自動検出**します。
> - 参画記録 → ヘッダーに「高階」「はる」があり、今日の日付を含むシートを自動採用（旧シートと自動で区別）
> - テーマ → ヘッダーに「メモ書きのテーマ」を含むシートを自動採用
> なので設定の書き換えは不要です。

## ステップ3：動作テスト（任意だが推奨）

1. エディタ上部の関数選択で **`getData`** を選び、**▶ 実行**
2. 初回は権限の確認が出る → **権限を確認 → 自分のアカウント → 詳細 → (安全でないページに移動) → 許可**
3. 下部の実行ログにエラーが出なければOK（データがJSONで取れている状態）

## ステップ4：ウェブアプリとして公開

1. 右上 **デプロイ → 新しいデプロイ**
2. 歯車⚙ → **ウェブアプリ** を選択
3. 設定：
   - 説明：`朝活トラッカー`
   - 次のユーザーとして実行：**自分**
   - アクセスできるユーザー：**全員**　← ⚠️ ここ重要（これにしないと画面から読み書きできません）
4. **デプロイ** をクリック
5. 表示される **ウェブアプリのURL**（`https://script.google.com/macros/s/........./exec`）をコピー

## ステップ5：URLを2つのファイルに貼る

`index.html` と `cockpit.html` の両方、冒頭近くにある以下の行を探して、
`""` の中にコピーしたURLを貼り付けます。

```js
const GAS_URL = "";
        ↓
const GAS_URL = "https://script.google.com/macros/s/........./exec";
```

（高階さん側で貼ってもOKですし、URLを教えてもらえれば私が貼ります）

## 完成！

- コックピットで朝活 → 完了画面で「記録する」 → **シートの今日の行に 1 が入る**
- トラッカー → **シートを読んで草グラフ・参加率を表示**
- 今日のテーマ → **シートの未完了テーマを自動表示**（✏️で手書き上書きも可）

---

## よくある質問

**Q. 伊藤さんの列はどうなる？**
A. 読み込み・書き込みとも**無視**します（列は残しますが触りません）。表示も高階・はる・竹花の3人だけ。

**Q. コードを修正したら？**
A. 再度 **デプロイ → デプロイを管理 → 編集（鉛筆）→ バージョン:新バージョン → デプロイ**。URLは変わりません。

**Q. テーマを「完了」にして次へ送りたい**
A. 今は手動でシートの「完了」列を TRUE にすればOK（次の未完了テーマが自動で出ます）。
   ボタンで完了にする機能（`themeDone`）もコードに入っているので、後で完了ボタンを画面に足せます。

**Q. スマホでも使える？**
A. 今のHTMLはPC（主催者）での画面共有運用が前提です。全員がスマホから各自開く形にするなら、
   このHTMLをWebにデプロイ（Vercel等）すれば可能です（次の発展ステップ）。
